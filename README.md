# NovelWriter Service Template (NodeJS)

A service template for NodeJS.

## System Requirements

* NodeJS >= 0.10

## Getting Started

Install dependencies:

```bash
$ npm install
```

Run the server:

```bash
$ node app
```

View the app at [http://localhost:3000](http://localhost:3000).
